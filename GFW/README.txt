HERE lies the framework Files
No Source Files here!!!
what is available

**glContext -->Creates a glWindow that is drawn to, wrapped in a class has "glut" like style where it has to be init first, then glinit, then add personal functions for window resize, keyhandling, mousehanlding



^^^TODO and UPDATE
**Util will have all utility functions in a few classes, save to file, timers, alterantive math classes, parsers etc
**gpuContext Will have a gpu class that should allow setting up of shaders, running them, getting the pictures from gpu loading shaders, errors and what not, should work in conjunction with glContext